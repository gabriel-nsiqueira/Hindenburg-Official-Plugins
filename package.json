{
  "name": "hindenburg-official-plugins",
  "private": true,
  "license": "GPL-3.0-only",
  "scripts": {
    "build-all": "yarn workspaces foreach run build",
    "update": "yarn workspaces foreach version patch",
    "publish-all": "yarn workspaces foreach --exclude hindenburg-official-plugins npm publish --access public"
  },
  "workspaces": [
    "packages/*"
  ]
}
